import { CustomFilterObject } from './custom-filter-object.model';

describe('CustomFilterObject', () => {
  it('should create an instance', () => {
    expect(new CustomFilterObject()).toBeTruthy();
  });
});
