export class CustomFilterObject {
    public option:string;
    public selected:boolean;

    constructor(option: string, selected: boolean) {
        this.option = option;
        this.selected = selected;
    }
}

